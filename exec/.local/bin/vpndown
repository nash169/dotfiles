#!/bin/sh

CONFIG=$(sudo -A ls /etc/wireguard | cut -d'.' -f1 | dmenu -i -l 10 -m 0) || { echo "User exit"; exit 0; }
sudo wg-quick down $CONFIG || { echo "Error: cannot disconnect from $CONFIG"; exit 1; } 
